var host = "10.10.30.188", port = "49329";
function getProperties() {
	console.log("Adding Properties for Websocket");
	var a = {
		host : host,
		port : port
	};
	console
			.log("Properties are set:Host is: " + host + " and Port is: "
					+ port);
	return a
};